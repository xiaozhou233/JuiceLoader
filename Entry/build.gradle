plugins {
    id 'java'
}

group = 'cn.xiaozhou233.entry'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
}

jar {
    archiveBaseName.set("entry")
    archiveVersion.set("")
    destinationDirectory.set(rootProject.layout.buildDirectory.dir("libs"))
}